{
  "name": "puck-off",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @puck-off/client",
    "build": "npm run build -w @puck-off/client",
    "preview": "npm run preview -w @puck-off/client"
  },
  "engines": {
    "node": ">=20"
  }
}
